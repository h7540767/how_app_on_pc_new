##############################################
程序介绍：casefactory ac

casefactory 用来生成用例的程序
用法：-a 后面跟app的数目，-p 后面跟pc的数目

ac 解题程序
##############################################

##############################################
文本文件介绍：wrap.cpp wrap.h ac.cpp ac.h main.cpp case.cpp

包裹函数 wrap.cpp wrap.h
对一些常用的库函数，系统调用进行了封装
一个随机数生成器myrand(a, b),随机生成一个位于a--b之间的数，包括a b

解题函数ac.cpp ac.h

用例生成case.cpp

主函数main.cpp
##############################################

##############################################
用例格式介绍

一共三个文件 pcinfor.csv appinfor.csv delay.csv

pcinfor.csv中第一行记录了两个数：app的数量和pc的数量

然后每行记录一个pc的信息，依次是pc的id，pc的型号，pc的内存

appinfor.csv 中每两行记录的是app的信息

第一行依次是app的id， app适配的机型数量，app的内存需求

第二行则是具体的机型号

delayinfor.csv 中每行记录的是pc到pc之间的时延(ps:pcA到pcB的时延 和 pcB到pcAp的时延 不一样)

其中内存数据范围是[1, 1024]
    时延数据范围是[0, 5000]
	型号数据范围是[1, 50]
##############################################

















